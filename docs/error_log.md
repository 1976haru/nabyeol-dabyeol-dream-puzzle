# 에러 로그

웹게임 개발 중 발생한 에러와 처리 내용을 기록합니다.

| 날짜 | 단계 | 에러 | 처리 |
| --- | --- | --- | --- |
| 2026-05-26 | 엔진 빌드 | `boardResolver.ts` 의 `dropBlocks` 에서 `const types = []` 가 빈 배열(evolving any)로 추론되어 블록 종류 리터럴 타입이 `string` 으로 넓어짐 → `TS2322` | 배열을 `const types: BlockType[] = []` 로 명시하고 null 체크를 지역 변수로 분리하여 해결. 빌드 성공 |

이후 단계에서 에러 없이 빌드/테스트 통과.
