import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nabyeol.dreampuzzle',
  appName: '나별다별 꿈퍼즐',
  webDir: 'dist',
  android: {
    backgroundColor: '#4263ebff',
  },
};

export default config;
